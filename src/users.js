
const createUser = ({
  email = '',
  familyName = '',
  givenName = ''
} = {}) => {
  // TODO: generate created time, add user to Db, return id

  return 1
}

const getUser = (id) => {
  // TODO: query Db for User

  return {}
}

module.exports = {
  createUser,
  getUser
}
